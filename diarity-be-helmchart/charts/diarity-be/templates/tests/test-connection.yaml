apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "diarity-be.fullname" . }}-test-connection"
  labels:
    {{- include "diarity-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "diarity-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
